#!/bin/sh
XDG_RUNTIME_DIR=/run/weston
TTY=1

export XDG_RUNTIME_DIR

if [ -d /proc/xen ]; then
	LANG=ja_JP.utf8
	SHELL=/usr/sbin/textui
	export SHELL LANG
	exec /usr/bin/weston-terminal
fi

#else
exec /usr/sbin/wb login tty$TTY

