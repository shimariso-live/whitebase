#!/bin/sh
if [ "$1" == "login" ]; then
        exec weston-terminal --shell=/usr/bin/wbui
fi
#else
if [ "%1" == "installer" ]; then
	exec weston-terminal --shell=/usr/bin/installer
fi
#else
exec /sbin/agetty -o '-p -- \u' - vt220
